name: Build and Test

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    name: Build and Test Java Maven Project
    uses: Prottay-Ray/workflow-hub/.github/workflows/java-maven-build.yml@main
